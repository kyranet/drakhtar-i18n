// #include "Utils/NumberMetadata.h"

// #include <gtest/gtest.h>

// TEST(UtilsNumberMetadata, getNumber) {
//   EXPECT_EQ(NumberMetadata::zero(), 0);
//   EXPECT_EQ(NumberMetadata::one(), 1);
//   EXPECT_EQ(NumberMetadata::two(), 2);
//   EXPECT_EQ(NumberMetadata::three(), 3);
// }
